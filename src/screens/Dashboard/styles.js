import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    marginTop: 12,
    marginLeft: 8,
    marginRight: 8
  },
  row: {
    display: 'flex',
    flexDirection: 'row'
  },
  icon: {
    fontSize: 18,
    marginRight: 6,
  }
});

export default styles;
