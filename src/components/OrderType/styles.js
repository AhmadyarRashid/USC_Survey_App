import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    display: 'flex',
    flexDirection: 'row',
    margin: 4,
  },
  optionView: {
    display: 'flex',
    flexDirection: 'row'
  },
  btn: {
    marginLeft: 0,
  }
});

export default styles;
